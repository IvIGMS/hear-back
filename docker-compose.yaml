version: '3.8'

services:
  db:
    image: postgres:16
    container_name: hear-db
    environment:
      POSTGRES_DB: hear
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5442:5432"
    volumes:
      - hear_data:/var/lib/postgresql/data

volumes:
  hear_data:
